'use strict';
var assert = require('assert');
//var alphaBetaSearch = require('../../../src/');
//var chessRules = require('chess-rules');

describe('random strategy', function () {
    it('must provide a score for each piece', function () {
        /**
         * TODO: Test for the strategy.
         */
        assert(true, 'Score is valid');
    });
});

describe('basic strategy', function () {
    it('must provide a score for each piece position', function () {
        /**
         * TODO: Test for the strategy.
         */
        assert(true, 'Score is valid');
    });
});
